<!-- <div ref:container tabindex="{tabindex}" id="{id}"></div>

<script>
	import ClassicEditor from "../../ckeditor/ckeditor";
	
	export default {
		oncreate() {
			const self = this;
			const { field } = self.get();

			ClassicEditor.create(self.refs.container, {
				simpleUpload: {
					// The URL that the images are uploaded to.
					uploadUrl: "/file/upload2",

					// Headers sent along with the XMLHttpRequest to the upload server.
					headers: {
					}
				},
				language: "en",
				image: {
					styles: [
						"alignLeft",
						"alignCenter",
						"alignRight"
					]
				}
			})
				.then(editor => {
					editor.setData(field.selected || {});
					editor.model.document.on("change:data", () => {
						// eslint-disable-next-line no-console
						console.log("The data has changed!");
						field.selected = editor.getData();
					});
				})
				.catch(error => {
					// eslint-disable-next-line no-console
					console.error("There was a problem initializing the editor.", error);
				});

			const formElement = this.refs.container.closest("form");
			formElement.addEventListener("submit", () => {
				if ((field.selected === "undefined" || field.selected === "" || field.selected == null) && field.metadata.required) {
					self.refs.input.parentElement.classList.add("divError");
				}
			});
		}
	};
</script>

<style>
</style> -->
